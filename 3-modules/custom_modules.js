// Modules - dividing the codes into modules for managing 
const names = require('./constant_module')
const sayHi = require('./utils')
const data = require('./alternative_module')
console.log(data)
sayHi(names.indresh)
sayHi(names.navaneeth)
sayHi(names.jhon)
sayHi(names.peter)